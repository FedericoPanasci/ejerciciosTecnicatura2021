#include <iostream>

using namespace std;
/*Ingresar los elementos de un arreglo o vector P, a continuación ingresar los de otro arreglo o vector Q.
Validar que la cantidad de elementos ingresados para cada uno de los vectores sea la misma y calcular:
a) La suma y la diferencia de los vectores.
b) El producto escalar. (Sumatoria de 1 a N de Pi * Qi)*/

int main () {

    return 0;
}